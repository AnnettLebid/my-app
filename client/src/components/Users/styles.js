import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  
  boxContainer: {
    marginTop: "40px",
    marginBottom: "50px",
    display: "flex",
    justifyContent: "space-between",
  }
}));